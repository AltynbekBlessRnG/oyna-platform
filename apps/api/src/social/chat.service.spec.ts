import type { AuthUser } from "@oyna/contracts";import { BadRequestException } from "@nestjs/common";import { DatabaseService } from "../database/database.service";import { ChatService } from "./chat.service";
describe("ChatService",()=>{const service=new ChatService(new DatabaseService()),user:AuthUser={id:"player",phone:"+77000000000",name:"Player",role:"player"};it("creates and returns a message",async()=>{const message=await service.create("cs2-kz",user,{text:"Ищу команду"});expect(message.text).toBe("Ищу команду");expect(await service.list("cs2-kz",user.id)).toContainEqual(message)});it("rejects an empty message",async()=>{await expect(service.create("cs2-kz",user,{text:"  "})).rejects.toBeInstanceOf(BadRequestException)});});
